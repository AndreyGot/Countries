{% extends 'base.html.twig' %}

{% block body %}
    <h1>Country creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="btn btn-info" href="{{ path('country_index') }}" role="button">Back to the list</a>
        </li>
    </ul>
{% endblock %}
